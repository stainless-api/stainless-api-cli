#!/usr/bin/env bash

set -e

ORIGINAL_DIR="$(pwd)"
PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

# Build the binary in the project root
cd "$PROJECT_ROOT"
go build -o /tmp/stl-dev ./cmd/stl

# Run it from the original directory
cd "$ORIGINAL_DIR"
/tmp/stl-dev "$@"
